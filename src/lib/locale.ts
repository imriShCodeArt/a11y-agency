/**
 * Default public locale for the marketing site (Hebrew-first).
 * When English support ships, derive `lang` / `dir` / theme direction from the active locale.
 */
export const defaultLocale = "he" as const;
export const defaultTextDirection = "rtl" as const;
